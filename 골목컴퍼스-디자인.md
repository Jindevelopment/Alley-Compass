# 골목 컴퍼스 — 웹 디자인 명세

> 서울 골목상권 생존 안정성 기반 AI 입지 추천 엔진 · 팀 AIM
> 파일: `골목 컴퍼스.dc.html` (편집용) · `골목-컴퍼스-standalone.html` (단일 파일 배포용)

---

## 1. 설계 전제

BASA 「소상공인 상권분석」의 화면 구조(떠 있는 조건 패널 · 지도 중심 · 우측 결과 패널)를
참조하되, **순서를 뒤집었다.**

| | BASA | 골목 컴퍼스 |
|---|---|---|
| 첫 입력 | 장소("혁신동") | 조건(업종·예산·타깃·성격·우선순위) |
| 지도의 역할 | 선택한 동을 보여줌 | 조건이 찾아낸 후보 5곳을 흩뿌림 |
| 결과 | 해당 지역 통계 나열 | 순위 + 추천 근거 + **반대 근거** |
| 숫자의 지위 | 표로 제시 | 검증 Tool 출처를 각 문장에 병기 |

---

## 2. 레이아웃

3열 고정 골격, 헤더 56px.

```
┌────────────────────────────────────────────────────────────┐
│ 헤더 56px — 로고 · 태그라인 · 데이터 범위 칩 · 계정         │
├──────────────┬───────────────────────┬─────────────────────┤
│ 조건 패널     │ 지도 (Leaflet + OSM)  │ 결과 패널            │
│ 372px         │ flex, min 304px       │ 452px               │
│ min 262px     │                       │ min 318px           │
│ 스크롤 + 하단 │ 범례(좌상) · 톤 토글  │ 순위 → 상세 연속 스크롤│
│ 고정 CTA      │ (우상) · 줌(우하)     │                     │
└──────────────┴───────────────────────┴─────────────────────┘
```

- 세 열 모두 `flex-shrink` 허용 + `min-width`. 좁은 미리보기(≈920px)에서도 지도가 죽지 않는다.
- 지도는 mount·resize 시 `invalidateSize()` → 컨테이너 크기에 비례한 padding으로 `fitBounds(maxZoom:14)`.
- OSM 저작자 표시(라이선스 필수)와 줌 컨트롤 위에 어떤 오버레이도 놓지 않는다.

---

## 3. 조건 패널 (좌)

장소가 아니라 조건을 받는 5단계. 단계 번호는 원형 뱃지.

1. **업종** — 4×2 타일 그리드. 선택은 테두리 링으로 표시(BASA의 아이콘 그리드 대응).
   선택 즉시 하단에 남색 경로 바 `음식 > 한식 > 한식(일반음식점)`.
2. **예산** — 3,000만 ~ 3억 슬라이더.
   바로 아래 호박색 주의 블록: *"공개 데이터셋에 보증금·임대료가 없습니다. 예산은 산술 검증까지만."*
3. **타깃 고객** — 칩 다중 선택 (20대 / 30~40대 직장인 / 가족 단위 / 학생 / 유동·관광)
4. **상권 성격** — 칩 다중 선택 (주거 밀집 / 직장가 / 대학가 / 역세권 / 전통시장 인접)
5. **가장 중요한 것** — 단일 선택 (생존 안정성 / 성장성 / 낮은 경쟁)

하단 고정 CTA **「나침반 돌리기 →」** · 보조문 `Top-5 · 평균 2.4초`.

---

## 4. 지도 (중앙)

- Leaflet 1.9.4 + OpenStreetMap 타일, `© OpenStreetMap contributors` 표기 유지.
- 핀은 divIcon 알약형: 선택 = 남색 채움 + 호박색 순위칩 + Score, 비선택 = 흰 바탕 + 호박색 테두리.
- 우상단 **일반 / 담백** 토글 — 담백은 타일 pane에 grayscale 필터.
- 좌상단 범례 + `model_version: heuristic-v0` (모델이 아직 휴리스틱임을 화면에서 숨기지 않음).

---

## 5. 결과 패널 (우)

**순위 리스트** — 순번 · 상권명 · 자치구 · 점포 수 · 점포당 배후수요 · Score 바 · 상위 %.

**선택 상권 상세**

| 블록 | 내용 |
|---|---|
| 상권 진단 4영역 | 수요 / 경쟁 / 성장 막대 + **비용은 해치 패턴 "데이터 미보유 — 점수를 만들지 않습니다"** |
| 시간대별 유동인구 | 6구간(00–06 … 21–24) 막대, 값은 k 단위 |
| 분기별 추이 | 매출(실선 남색) · 폐업률(점선 호박색) 5분기 스파크라인 |
| AI 근거 | 추천 3 / 반대 3 카드. 각 문장 아래 `verify_*()` 출처 표기, 상단에 `검증 Tool 6종 대조 · 정정 0건` |
| 액션 | PDF 리포트 · 후보 비교함에 담기 |
| 각주 | Score는 상권 × 업종 단위 지표 — 개별 점포 생존확률 아님 |

---

## 6. 화면에 새긴 4가지 설계 원칙

1. **숫자는 코드가 증명한다** → 모든 AI 문장에 검증 함수명을 붙인다.
2. **없는 데이터를 지어내지 않는다** → 비용 영역은 빈칸이 아니라 *명시적 미보유 표시*.
3. **부족을 추정으로 메우지 않는다** → 임차료 검증 실패는 `verified_by_data = False`로 노출.
4. **절대 점수보다 분포 내 위치** → Score 옆에 항상 `상위 N%`.

---

## 7. 시각 언어

| 토큰 | 값 | 용도 |
|---|---|---|
| 바탕 | `#F2F0EA` | 앱 배경 (따뜻한 오프화이트) |
| 패널 | `#FFFFFF` / `#FBFAF7` | 패널·카드 |
| 잉크 | `#1C1E22` / `#5A5F68` / `#8A9098` | 본문 3단계 |
| 경계 | `#E4E1DA` | 구분선 |
| 주색 | `#203A55` | CTA · 선택 · Score |
| 강조 | `#B5701F` / `#E9B44C` | 순위 · 상위 % · 폐업률 |
| 경고 | `#A33A31` | 반대 근거 |
| 검증 | `#2F6B4F` | 검증 뱃지 |

- 서체: **Pretendard Variable** (UI) + **IBM Plex Mono** (모든 수치·코드·출처)
- 수치를 모두 모노로 조판해 "계산된 값"과 "쓰인 문장"을 눈으로 구분시킨다.
- 라운드 8~10px, 그림자는 지도 오버레이에만.

---

## 8. 조정 가능한 항목 (Tweaks)

| 이름 | 타입 | 기본 |
|---|---|---|
| `resultCount` | 3–5 | 5 — 노출할 후보 수 |
| `showAiEvidence` | boolean | true — AI 근거 섹션 표시 |

---

## 9. 남은 연결

- 화면 데이터는 **샘플**이다. `/rank`, `/districts/{code}/detail`, `/agents` 응답 스키마에 맞춰
  `DISTRICTS` 상수를 교체하면 그대로 동작한다.
- `POST /report`(WeasyPrint)는 «PDF 리포트» 버튼에 아직 연결되지 않았다.
- Score 라벨은 LightGBM 학습 완료 시 `heuristic-v0` 칩만 바꾸면 된다.
