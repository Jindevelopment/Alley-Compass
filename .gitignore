# ── OS / Editor ─────────────────────────────────────
.DS_Store
.idea/
*.swp

# ── Secrets (NEVER commit) ──────────────────────────
.env
.env.*
!.env.example

# ── Python ──────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# ── ETL 데이터 (파이프라인이 재생성) ────────────────
alley_compass_etl/data/raw/
alley_compass_etl/data/processed/

# ── EDA 원본 덤프 (스크립트 재실행으로 재생성, 용량 큼) ──
DB_EDA/district_features_raw.csv

# ── 로컬 도구 상태 ──────────────────────────────────
.claude/

# ── Web (React/Vite/TypeScript) ─────────────────────
node_modules/
dist/
*.local
*.tsbuildinfo

# ── ML 산출물 (재현 가능, 용량 문제로 커밋 안 함) ────
ml/models/*.joblib
